name: phpstan

on: pull_request

jobs:
  vanilla:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run phpstan
        run: docker run -v `pwd`:/src/php -w /src/php ghcr.io/phpstan/phpstan analyse
